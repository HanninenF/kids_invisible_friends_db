###############################################
# 🌐 Databasanslutning
# Den här används av backend-servern för alla
# GET/POST och övriga databasfrågor i projektet.
###############################################
DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE_NAME"


###############################################
# 🧪 Shadow Database (valfritt)
# Den här används ENDAST av Prisma under migrationer
# (t.ex. `npx prisma migrate dev`) för att beräkna
# skillnader mellan schema-versioner.
#
# Behövs endast om du ska skapa eller köra nya migrationer
# lokalt. För vanlig utveckling och körning av backend
# krävs den inte.
###############################################
# SHADOW_DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE_NAME_shadow"
